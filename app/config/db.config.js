const config = {
    host: "localhost",
    port: 5432,
    database: "db_bootcamp",
    username: "postgres",
    password: "admin123",
    dialect: "postgres",
    logging: false,
}

module.exports = config;